# Микросервис 005
## 📋 Описание проекта

Высоконагруженная микросервисная система для обработки банковских транзакций в режиме реального времени с использованием event-driven архитектуры. Система обеспечивает обработку транзакций, управление балансами счетов и предоставление API для доступа к банковским данным.

## 🏗️ Архитектура

Проект построен на основе реактивной архитектуры с использованием:

- **Akka Streams** - потоковая обработка данных
- **Akka HTTP** - REST API и WebSocket endpoints
- **Akka Cluster** - распределенная система с горизонтальным масштабированием
- **Apache Kafka** - event streaming и обработка сообщений
- **Apache Cassandra** - хранилище временных рядов транзакций
- **Oracle Database** - основное хранилище банковских данных
- **KSQL** - потоковая обработка и агрегация данных

## ✨ Основные возможности

### REST API
- Получение балансов счетов по налоговому идентификатору
- Выгрузка транзакций по счету за период
- Формирование полной выписки по всем счетам налогоплательщика
- Health check endpoints для мониторинга

### WebSocket API
- Real-time обновления балансов счетов
- Push-уведомления о транзакциях
- Pub/Sub модель для подписки на события

### Обработка данных
- Streaming обработка транзакций из Kafka
- Обогащение транзакций данными из внешних систем
- Агрегация балансов на конец дня
- Обработка сбоев с автоматическими retry

## 🛠️ Технологический стек

**Язык:** Scala 2.13

**Фреймворки:**
- Akka 2.6+ (Streams, HTTP, Cluster)
- Akka Management & Bootstrap
- Alpakka Kafka
- Alpakka Cassandra
- Slick (для работы с Oracle)

**Инфраструктура:**
- Apache Kafka + Schema Registry (Avro)
- Apache Cassandra
- Oracle Database
- KSQL
- Kubernetes (опциональное развертывание)

**Библиотеки:**
- JSON4S / Jackson - сериализация
- Avro4s - работа с Avro схемами
- Logback/SLF4J - логирование

## 📊 Ключевые компоненты

### TaxTransactionStatement
Формирование полной выписки по всем счетам налогоплательщика за период с параллельной обработкой:
- Поиск всех счетов клиента
- Параллельная загрузка транзакций для каждого счета
- Обогащение данными из bank_statement
- Расчет балансов на начало и конец периода

### Kafka Consumers
Потоковая обработка событий из Kafka с:
- Автоматическим restart при сбоях
- Backoff стратегией для reconnect
- Avro десериализацией
- Группировкой сообщений для batch processing

### WebSocket Sessions
Real-time коммуникация с клиентами:
- Actor-based управление сессиями
- Pub/Sub для broadcast сообщений
- Graceful shutdown при разрыве соединения

### Stream Processing
- Supervision стратегии для обработки ошибок
- Throttling и backpressure
- Параллельная обработка с `flatMapMerge`
- Graceful degradation при недоступности внешних систем

### Конфигурация

```hocon
# application.conf
application {
  name = "banking-transaction-service"
  cluster-name = "banking-cluster"
  node.roles = "rest_api,websocket_api,stream_subscriber"
}

http {
  ip = "0.0.0.0"
  port = 8080
  ws_port = 8081
}

akka.discovery.method = "kubernetes-api" # или "config" для локального запуска
```

## 📡 Примеры использования API

### REST API

```bash
# Получение балансов по налоговому ID
GET /api/accbals/{taxId}

# Транзакции по счету за период
GET /api/cbs.trn/{account}/{startDate}/{endDate}

# Полная выписка по налоговому ID
GET /api/tax-transaction-statement/{taxId}/{startDate}/{endDate}
```

### WebSocket

```javascript
const ws = new WebSocket('ws://localhost:8081/ws');

// Подписка на обновления
ws.send(JSON.stringify({
  t: 'subscribe',
  channel: 'loans.periods'
}));

// Обработка входящих сообщений
ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  console.log('Received:', data);
};
```

## 🎯 Ключевые технические решения

### 1. Обработка ошибок
Многоуровневая система обработки ошибок:
- Stream-level supervision (Resume/Restart/Stop)
- Retry механизмы для внешних вызовов
- Circuit breaker для защиты от cascade failures

### 2. Оптимизация производительности
- Параллельная обработка с контролируемым параллелизмом
- Batch запросы к Cassandra
- Connection pooling для БД
- Кэширование часто запрашиваемых данных

### 3. Масштабируемость
- Stateless дизайн сервисов
- Akka Cluster для horizontal scaling
- Sharding для распределения нагрузки
- Kubernetes-ready с health checks

### 4. Мониторинг и отказоустойчивость
- Health check endpoints
- Structured logging
- Metrics для производительности
- Graceful shutdown

## 📈 Производительность

- **Throughput:** до 10K транзакций/сек на узел
- **Latency:** p99 < 100ms для API запросов
- **Availability:** 99.9% uptime в production

## 🔒 Безопасность

- Валидация входных данных
- Защита от SQL injection через параметризованные запросы
- Rate limiting на WebSocket соединения
- Логирование всех операций для аудита

## 📚 Структура проекта

```
src/main/scala/com/github/magkairatov/
├── Routes.scala                          # REST API endpoints
├── Server.scala                          # Точка входа приложения
├── TaxAccountTransactionStatement.scala  # Бизнес-логика выписок
├── WsSession.scala                       # WebSocket session management
├── WsMgr.scala                          # WebSocket менеджер
├── WsFlow.scala                         # WebSocket flow фабрика
├── Kafka_Flow*.scala                    # Kafka consumers
├── Kafka_Streams_Core.scala             # Kafka configuration
└── RestApiFlows_Supervision.scala       # Error handling strategies
```
